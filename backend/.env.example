# Backend env (copy to .env for local non-docker run)
DATABASE_URL=postgresql+psycopg://postgres:test@localhost:5432/mandorhub
JWT_SECRET=dev-secret-change-me
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=10080
# Dev: kembalikan kode OTP di response (JANGAN true di produksi)
OTP_DEV_MODE=true
CORS_ORIGINS=*
